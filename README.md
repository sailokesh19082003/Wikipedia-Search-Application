# Wikipedia-Search-Application
Developed custom Wikipedia search application where users can search and view curated results with detailed explanations • Fetched search results asynchronously from server using fetch GET HTTP API calls and opened results in new tabs
